import React from "react";
import { Button } from "@seoko/ui";

const Home = () => {
  return (
    <div>
      HOME
      <Button
        css={{
          color: "red",
        }}
      >
        버튼
      </Button>
    </div>
  );
};

export default Home;
