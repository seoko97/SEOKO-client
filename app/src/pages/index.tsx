import React from "react";
import { Button } from "@seoko/ui";

const Home = () => {
  return (
    <div>
      HOME
      <Button
        css={{
          color: "red",
        }}
        label="버튼"
      />
    </div>
  );
};

export default Home;
